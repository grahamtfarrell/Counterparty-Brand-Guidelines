/** Canvas display face — must match the family loaded in layout.tsx. */
export const CANVAS_TEXT_FONT =
	'"Instrument Serif", "Instrument Serif Fallback", Georgia, serif';

/** Mono grid step for stable column layout with proportional display font. */
export const GRID_FONT =
	'ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, "Liberation Mono", "Courier New", monospace';
